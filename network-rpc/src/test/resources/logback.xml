<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration>

    <!-- network-core log configuration file. upgrade to logback        -->
    <!--                                                                -->
    <!-- For professional support please see                            -->
    <!--    http://logback.qos.ch/documentation.html	                -->
    <!--    http://logback.qos.ch/manual/configuration.html             -->
    <!--    http://logback.qos.ch/manual/appenders.html                 -->
    <!--                                                                -->

<configuration debug="false">

    <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
        <resetJUL>true</resetJUL>
    </contextListener>

    <property name="log_pattern" value="%date{&quot;yyyy-MM-dd HH:mm:ss,SSS&quot;}%6p [%F:%L] - %m%n" />
    <property name="log_base_dir" value="./logs/%d{yyyyMMdd}" />

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <Append>true</Append>
        <encoder>
            <pattern>${log_pattern}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>WARN</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${log_base_dir}/file.log</fileNamePattern>
        </rollingPolicy>
    </appender>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <Target>System.out</Target>
        <encoder>
            <pattern>${log_pattern}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
    </appender>

    <appender name="DBG" class="ch.qos.logback.core.ConsoleAppender">
        <Target>System.out</Target>
        <encoder>
            <pattern>${log_pattern}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>DEBUG</level>
        </filter>
    </appender>

    <logger name="org.apache.niolex.network.cli" level="DEBUG">
		<appender-ref ref="DBG" />
	</logger>

    <logger name="org.apache.niolex.network.rpc" level="DEBUG">
		<appender-ref ref="DBG" />
	</logger>

    <root level="INFO">
        <appender-ref ref="FILE" />
    </root>
</configuration>